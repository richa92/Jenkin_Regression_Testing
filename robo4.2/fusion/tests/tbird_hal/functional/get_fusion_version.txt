*** Settings ***
Documentation
...    = Usage =
...    | pybot | -L DEBUG | get_fusion_version.txt |
...    | pybot | -L TRACE | -v FUSION_IP:<ip> | -v FUSION_USERNAME:<user> | -v FUSION_PASSWORD:<pass> | get_fusion_version.txt |
...    = Variables =
...    | ENCLOSURE | Required. Dictionary key from variables.py representing the desired enclosure. |

Library           FusionLibrary                # DVTs Robot Framework extensions

Resource          ../resources/defaults.txt
Resource          ../resources/fusion_api.txt

*** Test Cases ***
Get Fusion Version
	Login to Fusion via REST    ${FUSION_IP}    USERNAME=${FUSION_USERNAME}    PASSWORD=${FUSION_PASSWORD}
    ${Response}=    Fusion Api Get Appliance Version
    Log    Fusion Version: ${Response['softwareVersion']}    console=True
    Logout of Fusion Via REST