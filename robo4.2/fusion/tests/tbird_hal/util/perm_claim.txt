*** Settings ***
Documentation     Claim on enclosure manager in Fusion at the PERM layer
...    = Usage =
...    | pybot | -L DEBUG | -v ENCLOSURE:<name> | -v FUSION_IP:n.n.n.n | perm_claim.txt |
...    = Variables =
...    | ENCLOSURE | Required; Enclosure to use |
...    | FUSION IP | Required when enclosure is DCS; IP address of the FusionVM to use |
...    = Preconditions =
...       None

Library           RoboGalaxyLibrary
Variables         ../resources/variables.py    ${ENCLOSURE}
Resource          ../resources/defaults.txt    # DVT default variables
Resource          ../resources/perm_api.txt
Suite Setup       Login to Fusion Via REST
Suite Teardown    Logout of Fusion Via REST
Force Tags        DCS  HW

*** Test Cases ***
Perm Claim EM
    PERM Api Claim EM    ${True}      # Force the claim even if already claimed
