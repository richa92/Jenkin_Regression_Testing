*** Settings ** *
Documentation      Continuous Integration Tests for RoboGalaxy dev Branch
Resource           cerner_config.txt
Force Tags         Buildup
Test Setup         Load Test Data and Open Browser
Test Teardown      Logout And Close All Browsers


*** Keywords ***


*** Test Cases ***
FEX Uplink and Downlink port validation

    	Set Log Level    TRACE
    	Log Variables

 		${user}=	Get Data By Property	${TestData.users}	name	Administrator
		Fusion UI Login to Appliance   ${user[0].name}
        	          	
    	#Log to Console	verifying the interconnect uplink ports 
  		#${status}=  Fusion UI Verify Interconnect Uplink Port View      @{TestData.interconnects1}
        #run keyword if   '${status}'=='False'    log to console      "Uplinkport validation failed"
        
        Log to Console	verifying the interconnect downlink ports 
		${status}=  Fusion UI Verify Interconnect Downlinkport View Cisco      @{TestData.interconnects1}
        #run keyword if    '${status}'=='False'    log to console      "Downlinkport validation failed"
        
