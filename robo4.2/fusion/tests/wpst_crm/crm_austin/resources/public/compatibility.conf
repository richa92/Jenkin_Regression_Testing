#file: compatibility.conf
#Definition of data we need to capture for select compare

compatibility = [
    'credentials', 'criticalCount', 'enclosureGroupName', 'enclosureIp', 'enclosureName',
    'enclosureSerialNumber', 'highCount', 'itemCount', 'items', 'logicalInterconnectGroupName',
    'lowCount', 'mediumCount', 'migrationState'
]

credentials = [
    'oaIpAddress', 'vcmIpAddress'
]

itemCount = [
    'ethernetNetworkCount', 'fcFabricCount', 'interconnectCount', 'profileCount', 'serverCount'
]

items = [
    'issues', 'items', 'severity'
]

issues = [
    'description', 'recommendation', 'severity'
]
