*** Settings ***
Documentation      WPST CRM test for useLIGTask (WIP)	
...
...					
Suite Setup         Initialize Configuration
Suite Teardown      	

Resource	resource.txt         	

*** Test Cases ***

Execute WPST CRM Customized Test Scenario for applying the config using the
LIG and following the task 
	[Documentation]			Positive test - get user token 
	[Tags]			        POSITIVE 
 	# GET FIRST LOGIN SESSION
 	${login_token1}=		CIFIT Login			${APPLIANCE_IP}			${ADMIN_USER}		${ADMIN_PASSWORD}
 	#CIFIT Add Users			${login_token1}		${APPLIANCE_IP}					
