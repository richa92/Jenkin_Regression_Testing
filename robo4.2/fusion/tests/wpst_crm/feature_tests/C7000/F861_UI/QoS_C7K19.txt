*** Settings ***
Documentation       Feature Test: F861
Resource            OVAConfig.txt
#Force Tags         Buildup
Suite Setup         Load Test Data and Open Browser
Suite Teardown     Logout And Close All Browsers
Library            Dialogs
Library    String
Library         Collections

*** Test Cases ***

00.Log Into Fusion Appliance As Administrator
    [Documentation]    Log in to Appliance
    Log To Console    *********Log into Appliance as Administrator****************
    Load Test Data and Open Browser
    ${user} =    Get Data By Property    ${TestData.users}    name    Administrator
    fusion_ui_login_to_appliance    ${user[0].name}
    Log to Console    Successfully logged in as Administrator
    Log to Console    **************Initial Cleanup***************
    Fusion UI Remove All Enclosures
    Fusion UI Remove All Enclosure Groups
    Fusion UI Remove All Logical Interconnect Groups
    Log to Console    Initial Cleanup Successful

01. Create Logical Interconnect Group
    ${Status}=    fusion_ui_create_logical_interconnect_group      @{TestData.ligC71}
    Log to Console  ${Status}
    Run Keyword If  '${Status}' == 'True'   Log to Console  Logical Tnterconnect group created ELSE fail
    ${Status}=    Fusion UI Create Enclosure Group    @{TestData.encgroups}
    Log to Console  ${Status}
    Run Keyword  If '${Status}' == 'True'   Log to Console  Enclosure Group is created ELSE fail
    ${Status}=    Fusion UI Add Enclosure     @{TestData.enclosures_C7K}
    Log to Console  ${Status}
    Run Keyword If  '${Status}' == 'True'   Log to Console  Enclosure is added is created ELSE  fail

02. To verify LIG page
    ${Status}=    fusion_ui_validate_qos_configuration    @{TestData.ligs19}
    Log to Console  ${Status}
    Run Keyword If  '${Status}' == 'True'   Log to Console  verifiying the LIG page  ELSE   fail

03. Verify LI page
    ${Status}=    fusion_ui_validate_qos_configuration_in_li    @{TestData.li19}
    Log to Console  ${Status}
    Run Keyword If  '${Status}' == 'True'   Log to Console  verifying the LI page ELSE  fail

04. CLEANUP
    Fusion UI Remove All Enclosures
    Fusion UI Remove All Enclosure Groups
    Fusion UI Remove All Logical Interconnect Groups
    Log to Console  CLEANUP SUCCESSFULL
    Log to Console ALL STEPS COMPLETED SUCCESSFULLY






