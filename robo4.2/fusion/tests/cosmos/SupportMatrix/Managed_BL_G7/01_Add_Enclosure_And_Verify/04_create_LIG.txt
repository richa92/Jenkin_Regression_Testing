*** Settings ***
Documentation		Create LIG
Resource			../resource.txt
Test Setup			Load Test Data and Open Browser
Test Teardown		Logout and close all browsers
Force Tags			dcs  critical

*** Test Cases ***
Create Logical Interconnect Group: 
	[Tags]  lig
	Log into Fusion appliance as Administrator
	${blnlig} =		Fusion UI create logical interconnect group		@{TestData.ligs}
	Set Global Variable		${blnlig}
	Run Keyword If			'${blnlig}' == '${blnExpected}'	Log		All LIGs are created successfully.
	Run Keyword Unless		'${blnlig}' == '${blnExpected}'	Fail	Failed to create Some/all of the LIGs mentioned in data file.
