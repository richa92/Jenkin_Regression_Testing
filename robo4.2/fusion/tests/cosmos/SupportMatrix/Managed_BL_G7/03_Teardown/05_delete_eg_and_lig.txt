*** Settings ***
Documentation		Delete Enclosure and Logical Interconnect Group
Resource			../resource.txt
Test Setup			Load Test Data and Open Browser
Test Teardown		Logout and close all browsers
Force Tags			fusion  eg  dcs  critical


***Variables***
${blnExpected}			True


*** Test Cases ***
As an Administrator I want to delete Enclosure Group
	Log into Fusion appliance as Administrator
	${blnDeleteEnclgrps} =	Fusion UI remove all enclosure groups
	Run Keyword If	'${blnDeleteEnclgrps}' == '${blnExpected}'		Log		All Enclosure Groups are removed successfully from appliance.
	Run Keyword Unless		'${blnDeleteEnclgrps}' == '${blnExpected}'	Fail	Failed to remove Some/all of the Enclosure Groups from appliance.

As an Administrator I want to delete LIG from fusion appliance:
	Log into Fusion appliance as Administrator
	${blnAddedFCnets} =	Fusion UI remove all logical interconnect groups
	Run Keyword If	'${blnAddedFCnets}' == '${blnExpected}'			Log		All LIGs are removed successfully from appliance.
	Run Keyword Unless		'${blnAddedFCnets}' == '${blnExpected}'	Fail	Failed to remove Some/all of the LIGs from appliance.
