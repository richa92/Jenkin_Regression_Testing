*** Settings ***
Documentation		Delete San Manager
Resource			../resource.txt
Test Setup			Load Test Data and Open Browser
Test Teardown		Logout and close all browsers
Force Tags			fusion  sanmanager  storage  dcs  critical


***Variables***
${blnExpected}		True


*** Test Cases ***
As an Administrator I want to delete all SAN managers from my fusion appliance
	Log into Fusion appliance as Administrator
	${blnRemoveSAN} =	Fusion UI Remove All SAN Managers
	Run Keyword If	'${blnRemoveSAN}' == '${blnExpected}'			Log		All SAN Managers are removed successfully from appliance.
	Run Keyword Unless		'${blnRemoveSAN}' == '${blnExpected}'	Fail	Failed to remove Some/all of the SAN Managers from appliance.
