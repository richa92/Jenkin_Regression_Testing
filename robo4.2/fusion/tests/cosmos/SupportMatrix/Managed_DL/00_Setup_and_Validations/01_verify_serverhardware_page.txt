*** Settings ***
Documentation		Verfiy Server Hardware Page
Resource			../resource.txt
Test Setup			Load Test Data and Open Browser
Suite Teardown		Logout and close all browsers
Library				FusionLibrary
Library				RoboGalaxyLibrary
Variables			../../../../../FusionLibrary/ui/business_logic/general/base_elements.py
Variables			../../../../../FusionLibrary/ui/servers/serverhardware_elements.py
Force Tags			hardware


*** Test Cases ***
Verify Server Hardware Page
	Log into Fusion appliance as Administrator
	Fusion UI navigate to server hardware page
	Fusion UI select server hardware	${TestData.dlservers[0].name}
	Wait for element and click		${FusionServerHardwarePage.ID_LINK_HARDWARE}
	Sleep	5
	${server_model} =	Get Text	${FusionServerHardwarePage.ID_SERVER_MODEL}
	${server_hardware_type} =	Get Text	${FusionServerHardwarePage.ID_SERVER_HARDWARE_TYPE}
	${server_power} =	Get Text	${FusionServerHardwarePage.ID_ELEMENT_SERVER_POWER_ON}
	${server_ilo_ip} =	Get Text	${FusionServerHardwarePage.ID_ILO_IPV4}
	${server_hostname} =	Get Text	${FusionServerHardwarePage.ID_ILO_HOST_NAME}
	${server_serial_no} =	Get Text	${FusionServerHardwarePage.ID_SERVER_SER_NUM}
	${server_cpu} =	Get Text	${FusionServerHardwarePage.ID_LINK_CPU}
	${server_memory} =	Get Text	${FusionServerHardwarePage.ID_LINK_MEMORY}
	${dict}=	Create Dictionary	server model	${server_model}	server hardware type	${server_hardware_type}		server power	${server_power}	server ip	${server_ilo_ip}	server hostname		${server_hostname}	server serialno		${server_serial_no}	server cpu	${server_cpu}	server memory	${server_memory}
	Log		${dict}
	Wait for element and click		${FusionServerHardwarePage.ID_SELECT_ACTIVITY}
	Wait for element and click		${FusionServerHardwarePage.ID_LINK_PORTS}
