*** Settings ***
Resource            ../resource.txt
Suite Setup         Load Test Data and Open Browser
Suite Teardown      Logout and close all browsers
Force Tags          buildup  network

*** Test Cases ***
As an Administrator I want to delete storage volumes from Fusion
 	Log into Fusion appliance as Administrator
 	:FOR    ${volume}    IN    @{TestData.storagevolumes}
 	\    Fusion UI delete storage volumes  ${volume.name}  ApplianceAndStorage
	
As an Administrator I want to delete storage template from Fusion
 	Log into Fusion appliance as Administrator
 	:FOR    ${volumeTemp}    IN    @{TestData.storagevts}
 	\    Fusion UI delete storage volume template  ${volumeTemp.name}
 	
As an Administrator I want delete storage pool from Fusion
 	Log into Fusion appliance as Administrator
 	:FOR    ${pool}    IN    @{TestData.storagepools}
    \    Fusion UI remove storage pool    ${pool.name}
    
As an Administrator I want to remove storage system from Fusion
 	Log into Fusion appliance as Administrator
 	Fusion UI remove storage systems	@{TestData.storagesystems}
