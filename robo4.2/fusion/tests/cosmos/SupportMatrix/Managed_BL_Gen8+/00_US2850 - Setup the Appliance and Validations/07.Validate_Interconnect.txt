*** Settings ***
Resource            ../resource.txt
Suite Setup         Load Test Data and Open Browser
Suite Teardown      Logout and close all browsers
Force Tags          buildup interconnect

*** Test Cases ***
Verify details in the General, Alerts, Uplink port sections in interconnect page
	Log into Fusion appliance as Administrator
	fusion ui verify interconnect general view	${Interconnect_name}	${encl_name}	@{TestData.add_enclosure}
	fusion ui verify interconnect downlinkport view		${Interconnect_name}	${encl_name}	@{TestData.add_enclosure}
   	fusion ui verify interconnect activity view	${Interconnect_name}

   	
*** Test case need to be written for CC-12 interconnect-2 also as per the requirement ***   	