*** Settings ***
Resource            ../resource.txt
Suite Setup         Load Test Data and Open Browser
Suite Teardown      Logout and close all browsers
Force Tags          buildup Server profile



*** Test Cases ***
As an Administrator I want to Power ON the Server :
    Log into Fusion appliance as Administrator
    :FOR	${server}	IN	@{Testdata.power_on}
	\	fusion ui power on server		${server.name}	
	
As an Administrator I want to Power OFF the Server :
    Log into Fusion appliance as Administrator
    :FOR	${server}	IN	@{Testdata.power_off}
	\	fusion ui power off server		${server.name}		
	