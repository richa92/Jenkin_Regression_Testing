*** Settings ***
Resource            ../resource.txt
Suite Setup         Load Test Data and Open Browser
Suite Teardown      Logout and close all browsers
Force Tags          buildup Server profile

*** Test Cases ***

Create 1 profile booting from SAN on a DAS configuration (PXE as first on the boot order) and with a second FC connection to a FAS (shared data disk)
	Log into Fusion appliance as Administrator
	Fusion UI create Server Profile		@{TestData.profile_1} 