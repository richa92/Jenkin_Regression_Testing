*** Settings ***
Resource            ../resource.txt
Suite Setup         Load Test Data and Open Browser
Suite Teardown      Logout and close all browsers
Force Tags          buildup Enc

***Variables***
${blnExpected}		True

*** Test Cases ***
Add Enclosure:
    [Tags]  enc
	Log into Fusion appliance as Administrator
	${blnAddEncl} =	Fusion UI add enclosure   	@{TestData.add_enclosure}
	Set Global Variable		${blnAddEncl}
	Run Keyword If		'${blnAddEncl}' == '${blnExpected}'    Log		Enclosure added successfully in monitored mode.
	Run Keyword Unless		'${blnAddEncl}' == '${blnExpected}'    Fail	Failed to add some/all enclosures.	