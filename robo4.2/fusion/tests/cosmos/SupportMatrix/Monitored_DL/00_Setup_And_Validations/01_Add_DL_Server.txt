*** Settings ***
Resource            ../resource.txt
Suite Setup         Load Test Data and Open Browser
Suite Teardown      Logout and close all browsers
Force Tags          buildup  dl

*** Test Cases ***
US2092:Add a DL server
    Log into Fusion appliance as Administrator
    ${blnaddserver} =     fusion ui add server hardware    @{TestData.dlservers}
    Run Keyword If        '${blnaddserver}' == '${blnExpected}'    Log     All Server Hardware are added successfully.
    Run Keyword Unless    '${blnaddserver}' == '${blnExpected}'    Fail    Failed to add Server Hardware mentioned in data file.
