*** Settings ***

Library           RoboGalaxyLibrary

Resource          resource.txt


*** Keywords ***

Create LIG By Name
	[Arguments]    ${LIGname}
	${LIG} =  Get data by property  ${TestData.ligs}  name  ${LIGname}
	Fusion UI create logical interconnect group   ${LIG[0]}
	
Edit LIG By Name
	[Arguments]    ${LIGname}
	${LIG} =  Get data by property  ${TestData.editligs}  name  ${LIGname}
	Fusion UI edit logical interconnect group   ${LIG[0]}