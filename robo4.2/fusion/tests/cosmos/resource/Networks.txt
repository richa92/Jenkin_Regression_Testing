*** Settings ***

Library           RoboGalaxyLibrary

Resource          resource.txt


*** Keywords ***

Create FCNetwork By Name
	[Arguments]    ${FCname}
	${FC} =  Get data by property  ${TestData.fcnetworks}  name  ${FCname}
	Fusion UI create fc network   ${FC[0]}

Create EthernetNetwork By Name
	[Arguments]    ${Ethernetname}
	${Ethernet} =  Get data by property  ${TestData.networks}  name  ${Ethernetname}
	Fusion UI create ethernet network   ${Ethernet[0]}