*** Settings ***
Resource        resource/resource.txt
Library         RoboGalaxyLibrary
Library         FusionLibrary
Library         Collections
Library         String
Variables		../../../altair/AltairLibrary/ui/objectrepo/altair_elements_base_page.py
Variables		../../../altair/AltairLibrary/ui/objectrepo/altair_elements_servers_page.py

*** Keywords ***

Load Test Data Open Browser and Login
    Load test data and open browser
    Set Metadata
    Login to Fusion Via REST    ${IP}    ${USERNAME}    ${PASSWORD}
    Fusion UI Login To Appliance     Administrator

Fusion API Login By Name
    [Arguments]    ${name}
    ${user} =      Get Data By Property    ${TestData.users}   name    ${name}
    Log    Logging in as ${user[0].name}/${user[0].password}
    Login to Fusion Via REST    ${APPLIANCE_IP}     ${user[0].name}   ${user[0].password}

Fusion API Login By Role
    [Arguments]    ${role}
    ${user} =      Get Data By Property    ${TestData.users}   role    ${role}
    Login to Fusion Via REST    ${APPLIANCE_IP}     ${user[0].name}   ${user[0].password}
    
Fusion UI Log In As Network Administrator
    ${user} =    Get Data By Property  ${TestData.users}    role    Network administrator
    Fusion UI Login to Appliance    ${user[0].name}
    
Fusion UI Log In As Storage Administrator
    ${user} =    Get Data By Property  ${TestData.users}    role    Storage administrator
    Fusion UI Login to Appliance    ${user[0].name}

Get Job Time
    [return]        ${list[0]}      
    ${time}         GET TIME
    @{list}         Split String    ${time}

Log into Altair appliance as Administrator
	${logindata} =  Get data by property  ${TestData.altairappliances}  name  altair
	Open Browser         ${logindata[0].AltairApplianceUrl}       ${Browser}
    Altair_ui_login_appliance_console           ${logindata[0].user}	${logindata[0].password}
    Maximize Browser Window
	Wait For Element And Click		${AltairBasePage.ID_HELP_CONTROL}