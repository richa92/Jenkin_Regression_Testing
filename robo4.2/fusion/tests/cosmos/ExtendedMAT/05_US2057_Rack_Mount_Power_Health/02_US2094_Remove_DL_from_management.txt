*** Settings ***
Documentation		Remove DL Server
Resource			../resource.txt
Test Setup			Load Test Data and Open Browser
Test Teardown		Logout and close all browsers
Force Tags			teardown  dl  dcs


*** Test Cases ***
Remove DL Server
	Log into Fusion appliance as Administrator 
	${blnDeletedServerHardware} =	fusion ui remove server hardware		@{TestData.dlservers}
	Run Keyword If		'${blnDeletedServerHardware}' == '${blnExpected}'		Log		All Server Hardware are removed successfully.
	Run Keyword Unless		'${blnDeletedServerHardware}' == '${blnExpected}'	Fail	Failed to remove Server Hardware.
