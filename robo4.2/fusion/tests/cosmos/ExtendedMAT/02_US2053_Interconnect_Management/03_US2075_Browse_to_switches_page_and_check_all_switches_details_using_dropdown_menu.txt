*** Settings ***
Resource            ../resource.txt
Suite Setup         Load Test Data and Open Browser
Suite Teardown      Logout and close all browsers
Force Tags          ic  dcs

*** Test Cases ***
US2075:Browse to switches page and check all switches details, using the dropdown menu
	Log into Fusion appliance as Administrator
	fusion ui verify interconnect general view	${Interconnect_name}	${encl_name}	@{TestData.encl_existingEG}
	fusion ui verify interconnect downlinkport view		${Interconnect_name}	${encl_name}	@{TestData.encl_existingEG}
   	fusion ui verify interconnect activity view	${Interconnect_name}