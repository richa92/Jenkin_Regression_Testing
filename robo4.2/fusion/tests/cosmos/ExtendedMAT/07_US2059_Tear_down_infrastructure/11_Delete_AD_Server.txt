*** Settings ***
Resource 	    ../resource.txt
Test Setup		Load Test Data and Open Browser
Test Teardown	Logout and close all browsers
Force Tags      AD user  critical  teardown

*** variables ***
${ADServer}			AD1
${blnExpected}		True

*** Test Cases ***
Delete the AD User
	Log into Fusion appliance as Administrator
	${blnRemoveAdServer} =	Fusion UI Del Directory Server		@{TestData.ADdomainUsers}
	Run Keyword If    '${blnRemoveAdServer}' == '${blnExpected}'    Log	All AD Servers are removed successfully from appliance.
	Run Keyword Unless		'${blnRemoveAdServer}' == '${blnExpected}'    Fail	Failed to remove Some/all of the AD server from appliance.