*** Settings ***
Resource 	    ../resource.txt
Test Setup		Load Test Data and Open Browser
Test Teardown	Logout and close all browsers
Force Tags      datacenter  dcs  critical  teardown


***Variables***
${blnExpected}			True


*** Test Cases ***
US2104:Delete all the Data Centers in appliance
	Log into Fusion appliance as Administrator
	${blnDeleteDC} =  fusion ui delete all datacenters
	Run Keyword If    '${blnDeleteDC}' == '${blnExpected}'    log	All Data Centers are removed successfully from appliance.
	Run Keyword Unless		'${blnDeleteDC}' == '${blnExpected}'    Fail	Failed to remove Some/all of the Data Centers from appliance.
