*** Settings ***
Resource	        ../resource.txt
Test Setup			Load Test Data and Open Browser
Test Teardown		Logout and close all browsers
Force Tags          user  dcs  critical  teardown

***Variables***
${blnExpected}			True

*** Test Cases ***
As an Administrator I want to delete all users from my fusion appliance:
	Log into Fusion appliance as Administrator
	${blnUsers} =  Fusion UI Delete All Appliance Users
	Run Keyword If    '${blnUsers}' == '${blnExpected}'    Log		All Users are removed successfully from appliance.
	Run Keyword Unless		'${blnUsers}' == '${blnExpected}'    Fail	Failed to remove Some/all of the Users from appliance.
