*** Settings ***
Resource 	    ../resource.txt
Test Setup		Load Test Data and Open Browser
Test Teardown	Logout and close all browsers
Force Tags      rack  dcs  critical  teardown

*** Test Cases ***
Delete the Data Center
	Log into Fusion appliance as Administrator
	${blnRemoveRacks} =	Fusion ui remove all racks
	Run Keyword If    '${blnRemoveRacks}' == '${blnExpected}'    Log	All Racks are removed successfully from appliance.
	Run Keyword Unless		'${blnRemoveRacks}' == '${blnExpected}'    Fail	Failed to remove Some/all of the Racks from appliance.