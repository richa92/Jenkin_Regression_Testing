*** Settings ***
Documentation		Add SAN Manager to Appliance
Resource            ../resource.txt
Suite Setup         Load Test Data and Open Browser
Suite Teardown      Logout and close all browsers
Force Tags          sanmanager  storage  dcs  critical

*** Test Cases ***
As an Administrator I want to add SAN Manager to my fusion appliance
	Log into Fusion appliance as Administrator
	${blnSanMan} =	Fusion UI Add San Manager 	@{TestData.sanmanagers}
	Set Global Variable		${blnSanMan}
	Run Keyword If		'${blnSanMan}' == '${blnExpected}'    Log		All San Managers are created successfully.
	Run Keyword Unless		'${blnSanMan}' == '${blnExpected}'    Fail	Failed to create Some/all of the San Managers mentioned in data file.
