*** Settings ***
Documentation	SNMP Trap Forwarding
Resource 		../resource.txt
Test Setup		Load Test Data	${DataFile}
Test Teardown	Logout and close all browsers
Force Tags		deprecated  snmp

***Variables***
${blnExpected}		True

*** Test Cases ***
Setup Trap Forwarding
	[Tags]	setupfwd
	Open Browser	${ApplianceUrl}	${Browser}
	Log into Fusion appliance as Administrator
	Fusion UI Navigate to Settings Page
	Fusion UI Add SNMP Trap Forwarding Destination	@{TestData.snmp_settings}
