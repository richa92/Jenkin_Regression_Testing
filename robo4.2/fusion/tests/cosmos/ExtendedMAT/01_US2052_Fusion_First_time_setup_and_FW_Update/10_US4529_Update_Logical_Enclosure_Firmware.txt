*** Settings ***
Resource            ../resource.txt
Suite Setup         Load Test Data and Open Browser
Suite Teardown      Logout and close all browsers
Force Tags          datacenter  le  firmware  critical

*** Test Cases ***
Update Logical Enclosure Firmware
    Log into Fusion appliance as Administrator
    Fusion UI Power Off All Servers
    ${blnupdatefirmware} =    Fusion UI Update Logical Enclosure Firmware     @{TestData.update_le_firmware}
    Run Keyword If    '${blnupdatefirmware}' == '${blnExpected}'    Log    Update firmware is successfully completed.
    Run Keyword Unless    '${blnupdatefirmware}' == '${blnExpected}'    Fail    Update Firmware got failed