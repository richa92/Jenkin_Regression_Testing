*** Settings ***
Library		        Collections
Resource 	        ../resource.txt
Suite Setup         Load Test Data and Open Browser
Suite Teardown      Logout and close all browsers
Force Tags          hardware  dcs

*** Test Cases ***

Check Server Hardware types page:
	Log into Fusion appliance as Administrator
	${blnValidateData} =  Fusion UI Validate Server Hardware Types Page	@{TestData.verifyservertypes}
	Set Global Variable		${blnValidateData}
	Run Keyword If		'${blnValidateData}' == '${blnExpected}'    Log		Validation successfully.
	Run Keyword Unless	'${blnValidateData}' == '${blnExpected}'    Fail	Validation not successfully.