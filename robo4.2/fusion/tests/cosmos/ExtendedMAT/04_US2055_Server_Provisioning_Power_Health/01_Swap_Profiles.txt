*** Settings ***
Resource		../resource.txt
Test Setup		Load Test Data and Open Browser
Test Teardown	Logout and close all browsers
Force Tags		deprecated

*** Test Cases ***
US2091:Swap the profile of the host running windows 2012 to the one running ESXi and check if the settings are properly applied and if the OS keeps running
	Load Test Data and Open Browser
	Log into Fusion appliance as Administrator
	Fusion UI Power Off All Servers
	:FOR	${spprof}		IN	@{Testdata.editprofiles}
		\	fusion ui edit server hardware	${spprof}
	Sleep	2
	Fusion UI Navigate To Server Hardware Page
	Sleep	2
	# swapping of the servers which are unassigned
	:FOR	${spprof}		IN	@{TestData.assignprofiles2}
		\	Fusion UI assign server Profile		${spprof}
    Fusion UI Power On All Servers