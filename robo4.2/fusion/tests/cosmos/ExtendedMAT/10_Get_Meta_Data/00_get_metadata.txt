*** Settings ***
Documentation		Set Metadata for Fusion, Browser and OS Version
Resource			../resource.txt
Force Tags			critical  deprecated

*** Test cases ***
Set Oneview, browser and OS Metadata
	Load Test Data	${DATAFILE}
	${IP}=	Fetch From Right	${ApplianceUrl}		/
	${user} =	Get Data By Property	${TestData.users}	name	Administrator
	Set Fusion Version Metadata			${IP}	${user[0].name}		${user[0].password}
	Open And Configure Browser
	Set Browser Version Metadata
	Close all browsers