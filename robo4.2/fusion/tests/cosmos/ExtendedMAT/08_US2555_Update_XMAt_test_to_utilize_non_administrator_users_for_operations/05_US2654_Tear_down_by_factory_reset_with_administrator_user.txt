*** Settings ***
Documentation		Appliance Factory Reset
Resource			../resource.txt
Test Setup			Load Test Data and Open Browser
Test Teardown		Logout and close all browsers
Force Tags			deprecated

*** Test cases ***
Factory reset
	${user} =  Get data by property  ${TestData.users}  name  Administrator
	${user_name} =	Set Variable	${user[0].name}
	${appliance_password} =	Set Variable	${user[0].password}

	Log into Fusion appliance as Administrator
	${blnAppReset} =	Fusion UI Appliance Factory Reset	${user_name}	${appliance_password}
	Run Keyword If		'${blnAppReset}' == 'True'    Log		Factory Rest of appliance was done successfully
	Run Keyword Unless		'${blnAppReset}' == 'True'    Fail	Failed to Appliance factory reset