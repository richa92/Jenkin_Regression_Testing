*** Settings ***
Resource        ../resource.txt
Test Setup     	Load Test Data and Open Browser
Test Teardown  	Logout and close all browsers
Force Tags      critical  sp  dcs

*** Test Cases ***

US2078:Create 3rd profile
 	Log into Fusion appliance as Administrator
 	${blnCreateProf3} =  Fusion UI create Server Profile     @{TestData.profile3_US2078}
 	Set Global Variable		${blnCreateProf3}
	Run Keyword If		'${blnCreateProf3}' == '${blnExpected}'    Log		All Profiles are created successfully.
	Run Keyword Unless		'${blnCreateProf3}' == '${blnExpected}'    Fail	Failed to create Some/all of the Profiles mentioned in data file.

US2078:Create 4th profile
 	Log into Fusion appliance as Administrator
 	${blnCreateProf4} =  Fusion UI create Server Profile     @{TestData.profile4_US2078}
 	Set Global Variable		${blnCreateProf4}
	Run Keyword If		'${blnCreateProf4}' == '${blnExpected}'    Log		All Profiles are created successfully.
	Run Keyword Unless		'${blnCreateProf4}' == '${blnExpected}'    Fail	Failed to create Some/all of the Profiles mentioned in data file.

US2076:Create profile booting from a local disk (HardDisk as first on boot order), and one SAN connection to a non-bootable shared disk
 	Log into Fusion appliance as Administrator
 	${blnCreateProf} =  Fusion UI create Server Profile     @{TestData.profile_US2076}
 	Set Global Variable		${blnCreateProf}
	Run Keyword If		'${blnCreateProf}' == '${blnExpected}'    Log		All Profiles are created successfully.
	Run Keyword Unless		'${blnCreateProf}' == '${blnExpected}'    Fail	Failed to create Some/all of the Profiles mentioned in data file.

US20783:Create profile booting from a local disk (HardDisk as first on boot order), and one SAN connection to a non-bootable shared disk
 	Log into Fusion appliance as Administrator
 	${blnCreateProf2} =  Fusion UI create Server Profile     @{TestData.profile_US2077}
 	Set Global Variable		${blnCreateProf2}
	Run Keyword If		'${blnCreateProf2}' == '${blnExpected}'    Log		All Profiles are created successfully.
	Run Keyword Unless		'${blnCreateProf2}' == '${blnExpected}'    Fail	Failed to create Some/all of the Profiles mentioned in data file.