*** Settings ***
Resource        ../resource.txt
Test Setup		Load Test Data and Open Browser
Test Teardown  	Logout and close all browsers
Force Tags      critical  sp  dcs

*** Test Cases ***
US2081:Assign profiles to servers
 	Log into Fusion appliance as Administrator
 	${blnAssignProf} =  Fusion UI Assign Server Profile		@{TestData.assignprofiles}
 	Set Global Variable		${blnAssignProf}
	Run Keyword If		'${blnAssignProf}' == '${blnExpected}'    Log		Hardware is assigned successfully to Profile.
	Run Keyword Unless		'${blnAssignProf}' == '${blnExpected}'    Fail	Failed to assign hardware to Profile
