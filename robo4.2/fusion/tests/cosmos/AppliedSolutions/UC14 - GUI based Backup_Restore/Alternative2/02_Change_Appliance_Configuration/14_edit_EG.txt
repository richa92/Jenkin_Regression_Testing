*** Settings ***
Documentation		Edit Enclosure Group
Resource			../resource.txt
Test Setup			Load Test Data and Open Browser
Test Teardown		Logout and close all browsers
Variables			../../../../../../FusionLibrary/ui/servers/enclosuregroups_elements.py


*** Test Cases ***
Edit enclosure group to add new LIG group
	Log into Fusion appliance as Administrator
	fusion ui navigate to enclosure groups page
	${locator} =	Get UI Object	${FusionEnclosureGroupsPage.ID_ELMNT_ENC_GRP}	${TestData.encgroups[0].name}	
	wait for element	${locator}
	wait for element and click	${locator}
	wait for element and click	${FusionEnclosureGroupsPage.ID_ACTION_MAIN_BTN}
	wait for element and click	${FusionEnclosureGroupsPage.ID_LINK_EDIT}
	Sleep	5
	${visible} =	wait for element visible	${FusionEnclosureGroupsPage.ID_COMBO_SELECT_LOGICAL_INTERCONNECT}
	${click} =	wait for element and click	${FusionEnclosureGroupsPage.ID_COMBO_SELECT_LOGICAL_INTERCONNECT}
	${locator} =	Get UI Object	${FusionEnclosureGroupsPage.ID_COMBO_LIST_LOGICAL_SWITCH_TEMPLATE}	${TestData.ligs_new[0].name}
	wait for element and click	${locator}
	wait for element and click	${FusionEnclosureGroupsPage.ID_BTN_ENCLOSURE_GROUP_CREATE}
