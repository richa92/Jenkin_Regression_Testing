*** Settings ***
Documentation		Create AD domain user to Altair
Resource			../resource.txt
Library				AltairLibrary
Test Setup			Load Test Data	${DataFile}


*** Test Cases ***
Create Infrastructure and Backup Domain User
	Log into Altair appliance as Administrator
	Maximize Browser Window
	: FOR	${user}	IN		@{TestData.ADdomainUsers}
	\	Altair UI go to users and groups page
	\	Altair UI add active dir group	${user.userName}	${user.userPswd}	${user.egroup}	${user.role}
	Logout and close all browsers
