*** Settings ***
Documentation		Create extra volume Edit profile and add extra Volume
Resource 			../resource.txt
Test Setup         Load Test Data and Open Browser
Test Teardown      Logout and close all browsers

*** Test Cases ***
Add storage volume
	Log into Fusion appliance as Administrator
	Fusion UI create storage volumes		@{TestData.addExtraVolume_host1}

Edit profile and add volume
	Log into Fusion appliance as Administrator
	Fusion UI Power Off Server		${Testdata.profile_gen8[0].server}
	Fusion UI Edit Server Profile		@{TestData.edit_profile}
