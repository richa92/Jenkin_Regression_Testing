*** Settings ***
Resource        ../resource.txt
Test Setup     	Load Test Data and Open Browser
Test Teardown  	Logout and close all browsers


***Variables***
${blnExpected}			False

***TestCase***
Attempt to delete server profile template
    Log into Fusion appliance as Administrator
    ${blnDeleteprofile} =  Fusion UI Delete Profile Template	${Testdata.template_profile[0].templ_name}
    Run Keyword If    '${blnDeleteprofile}' == '${blnExpected}'    log	 Failed to remove Some/all of the server profiles templates from appliance.
    Run Keyword Unless		'${blnDeleteprofile}' == '${blnExpected}'    Fail	server profiles templates are removed successfully from appliance.
