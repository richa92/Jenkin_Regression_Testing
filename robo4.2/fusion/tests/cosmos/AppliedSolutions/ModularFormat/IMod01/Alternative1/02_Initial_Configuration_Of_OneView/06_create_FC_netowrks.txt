*** Settings ***
Documentation		Create FC Networks
Resource			../resource.txt
Test Setup			Load Test Data and Open Browser
Test Teardown		Logout and close all browsers
Force Tags			CC_01	CC_03

*** Test Cases ***
Create FC Networks:
	Log into Fusion appliance as Administrator
	${blnFC} =	Fusion UI create fc network		@{TestData.fcnetworks}
	Set Global Variable		${blnFC}
	Run Keyword If			'${blnFC}' == '${blnExpected}'    Log		All FC are created successfully.
	Run Keyword Unless		'${blnFC}' == '${blnExpected}'    Fail	Failed to create Some/all of the FC mentioned in data file.
