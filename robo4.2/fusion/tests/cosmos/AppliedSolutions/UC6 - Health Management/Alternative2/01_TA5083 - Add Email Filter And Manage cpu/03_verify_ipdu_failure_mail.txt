*** Settings ***
Documentation		Verify Ipdu Failure in Email
Resource            ../resource.txt
Test Setup			Load Test Data and Open Browser
Test Teardown		Logout and close all browsers
Force Tags          critical


*** Test Cases ***
Check for the Email is received for ipdu failure trap generated
	[Documentation]    Verifies the subject and body lines of the previously sent emails
    Connect To POP Mail Server      ${PopServer}    ${PopUser}    ${PopPassword}    use_ssl=True
    :FOR   ${Message}    in     @{Messages}
    \    Log    Validating Message ${Message['index']}
    \    Mail Message Subject Should Match    ${Message['index']}    ${Message['subject']}
    \    Mail Message Body Should Match    ${Message['index']}    ${Message['body']}
    