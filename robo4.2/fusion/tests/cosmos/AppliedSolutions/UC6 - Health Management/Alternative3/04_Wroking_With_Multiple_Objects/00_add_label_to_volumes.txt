*** Settings ***
Documentation		Add Label To Volume
Resource			../resource.txt
Test Setup          Load Test Data and Open Browser
Test Teardown       Logout and close all browsers

*** Test Cases ***
Add Label To Volumes
	Log into Fusion appliance as Administrator
 	${addlabel}=	Fusion UI Add Label To Volume	@{TestData.volume_label}
	Run Keyword If    '${addlabel}' == '${blnExpected}'		Log		Label is successfully added to the volume
	Run Keyword Unless    '${addlabel}' == '${blnExpected}'		Fail	Failed to add label to the volume
