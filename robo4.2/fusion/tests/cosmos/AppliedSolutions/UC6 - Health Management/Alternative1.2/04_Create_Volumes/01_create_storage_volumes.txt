*** Settings ***
Documentation		Add Storage volumes
Resource          	../resource.txt
Test Setup         	Load Test Data and Open Browser
Test Teardown      	Logout and close all browsers


*** Test Cases ***
As an Administrator I want add storage volumes to Fusion
	Log into Fusion appliance as Administrator
	${volumes} =	Set Variable	@{TestData.storagevolumes}
	${volumes_count} =	Get Length	${volumes}
	:FOR	${i}	IN RANGE	1	${volumes_count}
	\	${blnAddvolume} =  Fusion UI create_storage_volumes	${volumes[${i}]}
	\	Run Keyword Unless		'${blnAddvolume}' == 'True'	Fail	Failed to add Some/all of the Storage volumes
