*** Settings ***
Documentation		Add Storage System
Resource          	../resource.txt
Test Setup         	Load Test Data and Open Browser
Test Teardown      	Logout and close all browsers


*** Test Cases ***
Update Profile from Server Profile Template
	${serverProfile} =	Set Variable	${Testdata.update_profile_from_template[0]}

	Log into Fusion appliance as Administrator
	${blnUpdateProfFromTemp} =  Fusion UI Update Profile From Template	${serverProfile.prof_name}
	Run Keyword If		'${blnUpdateProfFromTemp}' == 'True'    Log		Server profile update successfully from template
	Run Keyword Unless		'${blnUpdateProfFromTemp}' == 'True'	Fail	Failed to update profile from template
	