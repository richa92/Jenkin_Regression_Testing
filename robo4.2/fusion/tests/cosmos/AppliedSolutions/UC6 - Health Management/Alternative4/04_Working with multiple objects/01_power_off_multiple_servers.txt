*** Settings ***
Documentation		Select and power off multiple profiles
Resource			../resource.txt
Test Setup			Load Test Data and Open Browser
Test Teardown       Logout and close all browsers


*** Test Cases *** 
Select and power off multiple profiles
	Log into Fusion appliance as Administrator
	${powerOffStatus} =		Fusion UI Power Off Server Profile	@{TestData.profile_list}
	Run Keyword If    '${powerOffStatus}' == '${blnExpected}'		Log		Successfully powered off profiles
	Run Keyword Unless    '${powerOffStatus}' == '${blnExpected}'		Fail	Failed to powered off profiles