*** Settings ***
Documentation		Select and delete multiple profiles
Resource			../resource.txt
Test Setup			Load Test Data and Open Browser
Test Teardown      Logout and close all browsers


*** Test Cases *** 
Select and delete multiple profiles
	Log into Fusion appliance as Administrator
	${deleteprofile} =		Fusion UI Delete Server Profile		@{TestData.profile_list}
	Run Keyword If    '${deleteprofile}' == '${blnExpected}'		Log		Successfully selected and deleted multiple profiles 
	Run Keyword Unless    '${deleteprofile}' == '${blnExpected}'		Fail	Failed to select multiple profiles and delete the same