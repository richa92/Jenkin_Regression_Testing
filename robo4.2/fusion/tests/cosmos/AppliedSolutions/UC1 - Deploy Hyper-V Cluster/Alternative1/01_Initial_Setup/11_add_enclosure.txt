*** Settings ***
Documentation		Add Enclosure
Resource 	        ../resource.txt
Test Setup          Load Test Data and Open Browser
Test Teardown       Logout and close all browsers
Variables			../../../../../../FusionLibrary/ui/networking/logicalinterconnects_elements.py


*** Test Cases ***
Add Enclosure to Appliance
	Login as a Infrastructure administrator of AD user
	${blnAddEnclr} =	Fusion UI add enclosure   	@{TestData.enclosures}
	Run Keyword If		'${blnAddEnclr}' == '${blnExpected}'    Log		All Enclosures are added successfully.
	Run Keyword Unless		'${blnAddEnclr}' == '${blnExpected}'    Fail	Failed to add Some/all of Enclosures

Check Enclosure is Updated Automatically in LI
	Login as a Infrastructure administrator of AD user
	Fusion UI Navigate To Logical Interconnects Page
	${power} =	Get Ui Object		${FusionLogicalInterconnectsPage.ID_ELEMENT_LI_SELECT_NAME}	${TestData.enclosures[0].liname}
	${blnAddEnclr} =	wait for element visible	${power}
	Run Keyword If		'${blnAddEnclr}' == '${blnExpected}'    Log		Enclosure is Updated Automatically in LI.
	Run Keyword Unless		'${blnAddEnclr}' == '${blnExpected}'    Fail	Failed to Updated Enclosure Automatically in LI