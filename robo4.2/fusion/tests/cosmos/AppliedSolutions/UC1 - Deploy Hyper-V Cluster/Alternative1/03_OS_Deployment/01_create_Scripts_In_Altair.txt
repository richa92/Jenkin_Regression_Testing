*** Settings ***
Documentation		Create Scripts in ALtair
Resource			../resource.txt
Test Setup			Load Test Data	${DataFile}
Test Teardown		Logout and close all browsers
Library				OperatingSystem


*** Test Cases ***
Create script for disabling firewall
	Log into Altair appliance as Administrator
	: For	${script}	In	@{TestData.scripts}
	\	${name} =	set Variable	${script.name}
	\	${description} =	set variable	${script.description}
	\	${content} =	set variable	${script.content}
	\	${script_type} =	set variable	${script.script_type}
	\	${action} =		set variable	${script.action}
	\	Altair Ui Go To Scripts Page
	\	altair ui create script		${name}	${description}	${content}	${script_type}	${action}
