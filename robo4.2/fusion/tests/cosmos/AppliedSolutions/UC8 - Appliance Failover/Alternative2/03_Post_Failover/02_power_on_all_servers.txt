*** Settings ***
Documentation		Power on the servers
Resource 	        ../resource.txt
Test Setup          Load Test Data and Open Browser
Test Teardown       Logout and close all browsers


*** Test Cases ***
Select and power on profile
	Login as a Infrastructure administrator of AD user
	${powerOnStatus} =		Fusion UI Power On Server Profile	@{TestData.profileon_postfailure}
	Run Keyword If    '${powerOnStatus}' == '${blnExpected}'		Log		Successfully powered on profiles
	Run Keyword Unless    '${powerOnStatus}' == '${blnExpected}'		Fail	Failed to powered on profiles