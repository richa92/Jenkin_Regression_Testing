*** Settings ***
Documentation		Configures an appliance with resources found in supplied data file. Pass in specific tags with pybot -i <tag(s)> to limit processing
...  TAGS:          CONFIGURE, LOGIN, CREATE SAN
Library                         FusionLibrary
Library                         RoboGalaxyLibrary
Library                         OperatingSystem
Library                         BuiltIn
Library                         Collections
Library                         XML
Library                         String
Library                         json

Resource						../../../Resources/api/fusion_api_resource.txt

Variables 		    			./data_variables.py
*** Variables ***
${X-API-VERSION}			    200
${APPLIANCE_IP}     			10.33.1.66
${one}                          1
${two}                          2
${three}                        3
${DATA}                         data_variables.py
${VERBOSE}                      False
*** Test Cases ***

Login
    [Tags]    CONFIGURE  LOGIN
	${resp} =   Fusion Api Login Appliance 		${APPLIANCE_IP}		${admin_credentials}

#Add Server Hardware Async
#	Set Log Level	TRACE
#	${server_hardwares} =	Get Variable Value	${server_hardwares}
#	${expected_server_hardwares} =	Get Variable Value	${expected_server_hardwares}
#	Run Keyword If	${server_hardwares} is not ${null}		Add Server Hardware Async  ${server_hardwares}  ${TRUE}  ${expected_server_hardwares}

Remove Server Hardware Async
	Set Log Level	TRACE
	Remove All DL Server Hardware Async  ${TRUE}

#Add Storage Volume Async
#	Set Log Level	TRACE
#	${storage_volumes} =	Get Variable Value	${storage_volumes}
#	${expected_storage_volumes} =	Get Variable Value	${expected_storage_volumes}
#	Run Keyword If	${storage_volumes} is not ${null}		Add Storage Volumes Async  ${storage_volumes}  ${TRUE}  ${expected_storage_volumes}

#Remove Storage Volume
#	Set Log Level	TRACE
#	Remove All Storage Volumes Async  ${TRUE}

#Add storage Pool Async
#	Set Log Level	TRACE
#	${storage_pools} =	Get Variable Value	${storage_pools}
#	${expected_storage_pools} =	Get Variable Value	${expected_storage_pools}
#	Run Keyword If	${storage_pools} is not ${null}		Add Storage Pool Async  ${storage_pools}  ${TRUE}  ${expected_storage_pools}

#Remove Storage Pools Asnyc
#	Set Log Level	TRACE
#	Remove All Storage Pools Async  ${TRUE}

#Add Storage System Async
#	Set Log Level	TRACE
#	${storage_systems} =	Get Variable Value	${storage_system_new}
#	${expected_storage_systems} =	Get Variable Value	${expected_storage_system}
#	Run Keyword If	${storage_systems} is not ${null}		Add Storage System from variable Async  ${storage_systems}  ${TRUE}  ${expected_storage_systems}

#Remove Storage System
#	Set Log Level	TRACE
#	Remove All Storage System Async  ${TRUE}

#Add SAN Managers Async
#	[Tags]    CONFIGURE  CREATE  SAN MANAGERS
#	Set Log Level	TRACE
#	${san_managers} =	Get Variable Value	${san_managers}
#	${expected_san_managers} =	Get Variable Value	${expected_san_managers}
#	Run Keyword If	${san_managers} is not ${null}			Add SAN Managers Async		${san_managers}  ${TRUE}  ${expected_san_managers}

#Remove SAN Managers Async
#	Set Log Level	TRACE
#	Remove All San Managers Async  ${TRUE}