*** Settings ***
Documentation     Test Comware Command Alias - US30876
...    = Usage =
...    | pybot | -L DEBUG | -v SWITCH_IP:<Your-Switch-IP> | command_alias.txt | 
...    = Variables =
...    | SWITCH IP | Required; IP address or hostname of the Comware Switch |
...    = Preconditions =
...    None

Library           RoboGalaxyLibrary                                     # DVTs Robot Framework extensions
Library           FusionLibrary                                         # DVTs Robot Framework extensions
Library           String
Library           Collections
Resource         ../resources/defaults.txt
#Resource          ../resources/fusion_api.txt                          # Comet-DVT keyword extensions
Resource          ../resources/switch.txt                               # Comet-DVT keyword extensions
Force Tags        CometTest    LEVEL0                                   # All test cases get these tags
Suite Setup       Login to Switch via SSH    ${SWITCH_IP}
Suite Teardown    Logout of Switch via SSH

*** Variables ***
${systemView_prompt}      ]

*** Test Cases ***
	
Create and verify command-alias
     Switch Execute Command    system-view    ${systemView_prompt}
	 Set Suite Variable    ${SWITCH_PROMPT}    ${systemView_prompt}
     Switch Execute Command    command-alias mapping display show     
     ${display_out}=    Switch Execute Command     display command-alias     
	 ${show_out}=    Switch Execute Command     show command-alias      
	 Should Not Be Equal As strings    ${display_out}    ${show_out}
	 
Enable and verify command-alias

	 Switch Execute Command    command-alias enable    
	 ${display_out}=    Switch Execute Command     display command-alias    
	 ${show_out}=    Switch Execute Command     show command-alias     
	 Should Be Equal As Strings     ${display_out}    ${show_out}
	 
Disable and verify command-alias

	 Switch Execute Command    undo command-alias enable     
	 ${display_out}=    Switch Execute Command     display command-alias         
	 ${show_out}=    Switch Execute Command     show command-alias     
	 Should Not Be Equal As strings     ${display_out}    ${show_out}
	 
Delete Command-alias

     Switch Execute Command    undo command-alias mapping display     
	 ${out}=    Switch Execute Command     display command-alias      
	 Should Not Contain    ${out}    show
	 Logout of Switch via SSH