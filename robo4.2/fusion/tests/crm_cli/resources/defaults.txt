*** Settings ***
Documentation     Default Variables 
...               = Usage =
...               | Resource | ../resources/defaults.txt |

*** Variables ***
# Fusion default credentials
${FUSION_IP}                  unknown          # Fusion Appliance IP Address
${FUSION_USERNAME}            Administrator    # Fusion Appliance Username 
${FUSION_PASSWORD}            hpvse123         # Fusion Appliance Password 
${FUSION_SSH_USERNAME}        root             # Fusion SSH Username 
${FUSION_SSH_PASSWORD}        hpvse1           # Fusion SSH Password 
${FUSION_FACTORY_PASSWORD}    admin            # Fusion Factory Default Password
${FUSION_PROMPT}              #
${FUSION_TIMEOUT}             300

# Enclosure Credentials
${ENC_IP}                     unknown          # OA IP Address
${ENC_USERNAME}               dcs              # OA Username
${ENC_PASSWORD}               dcs              # OA Password

# iLO default credentials
${ILO_IP}                     unknown          # iLO IP Address
${ILO_USERNAME}               Administrator
${ILO_PASSWORD}               hpvse123
${ILO_PROMPT}                 hpiLO->
${ILO_TIMEOUT}                300

# Switch default credentials
${SWITCH_IP}                  unknown          # Switch IP Address
${SWITCH_USERNAME}            Administrator
${SWITCH_PASSWORD}            hpvse123
${SWITCH_PROMPT}              >
${SWITCH_TIMEOUT}             300

# Gui/Selenium defaults
${BROWSER}                    firefox
${FUSION_GUI_DELAY}           5


# Default Fusion entity names
${LIG_NAME}                   LIG
${LI_NAME}                    LI
${I_NAME}                     HP VC Flex-10/10D Module
${NET_NAME}                   NW1
${NET_ID}                     101
${ENC_NAME}                   Encl1
${ENC_GROUP_NAME}             Encl-Grp1
${SERVER_PROFILE_NAME}        Profile 1
