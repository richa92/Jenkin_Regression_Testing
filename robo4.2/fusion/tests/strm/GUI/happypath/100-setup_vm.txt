*** Settings ***
Documentation   This test will setup the environment for the other tests in this path to use.

Resource          ${CURDIR}\\..\\resources.txt	

Suite Setup      Load Test Data Open Browser Login Fusion
Suite Teardown   Logout and Close All Browsers

Force Tags       GUI Func Happy path


*** Variables ***
${user}             Administrator
${version}
#${ApplianceUrl}     https://16.71.130.11

*** Test Cases ***

++100A-Setup configuration on VM for testing
    [Documentation]		Setup VM - Using GUI, Add SAN manager, Ethernet network and FC network to VM
    Fusion UI Delete FC Network            @{TestData.fcnetworks}
    ${version} =   Fusion UI Get Appliance Version
    Fusion UI Add San Manager            ${version}   @{TestData.sanmanagers}
	Fusion UI Create Ethernet Network	 @{TestData.networks}
	Fusion UI Create FC Network	         @{TestData.fcnetworks}
      
