*** Settings ***
Documentation   Negative Create volume snapshot will create the storage system and volumes. It will then try to create the volume
...             snapshot.

Resource          ${CURDIR}\\..\\resources.txt

Suite Setup      Load Test Data Open Browser Login Fusion
Suite Teardown   Logout and Close All Browsers

Force Tags       GUI Func negative path


*** Variables ***
${user}       Administrator
#${ApplianceUrl}     https://16.71.130.135



*** Test Cases ***

++N310A-Create volume snapshot
     Fusion UI Create Storage Volumes   @{TestData.strm.createvolumeforsnapshots}
     Fusion UI Create Snapshots Storage Volume   @{TestData.strm.negativecreatesnapshots}

++N310B-Promote volume snapshot
     Fusion UI Create Snapshots Storage Volume   @{TestData.strm.volumesnapshots}
     Fusion UI Create Volume Using Snapshot   @{TestData.strm.negativepromotesnapshots}