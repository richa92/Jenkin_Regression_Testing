*** Settings ***
Documentation   ENCLn0010 - Cannot Monitor an enclosure that already Managed by own appliance
Library         robot.api.logger
Resource        ../resource.txt
Test Setup      Load Multi Test Data and Open Browser
Test Teardown	Pause And Close Browser


*** Variables ***
${user}         Administrator


*** Test Cases ***
As an Administrator I cannot add an monitored enclosure which is being managed by own appliance
    Fusion UI Login To Appliance    ${user}
    ${data}=                            Get Data By Xpath   //ENCLn0010/enclosure
    ${rc}=                              Fusion UI Validate Should Not Readd Enclosure           @{data.enclosure}
    Should Be True      ${rc}   Failed to validate appliance should not re-add monitored enclosure which already managed by itself

