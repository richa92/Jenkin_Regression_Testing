*** Settings ***
Documentation   ENCLp0002-- Add Enclosure with IPv4 and discover hardware ( FQHN) exist EG - as SA
Library         robot.api.logger
Resource        ../resource.txt
Test Setup      Load Multi Test Data and Open Browser
Test Teardown	Pause And Close Browser


*** Variables ***
${user}         Administrator


*** Test Cases ***
As an Server Administrator I can add an managed enclosure by hostname and exist EG
    Fusion UI Login To Appliance    ${user}
    ${data}=                            Get Data By Xpath   //ENCLp0002a/enclosure
    ${rc}=                              Fusion UI Add Enclosure           @{data.enclosure}
    ${rc}=                              Fusion UI Remove Enclosure           @{data.enclosure}
    Should Be True      ${rc}   Failed to add an managed enclosure by hostname and exist EG

