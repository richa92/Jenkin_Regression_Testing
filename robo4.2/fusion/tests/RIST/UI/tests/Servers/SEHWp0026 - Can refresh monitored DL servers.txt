*** Settings ***
Documentation   SEHWp0026 - Can refresh monitored DL servers
Library         robot.api.logger
Resource        ../resource.txt
Test Setup      Load Test Data and Open Browser
Test Teardown	Pause And Close Browser


*** Variables ***
${user}         Administrator


*** Test Cases ***
As an Administrator I want to refresh a server hardware which is in Monitored
    Fusion UI Login To Appliance    ${user}
    ${rc}=                              Fusion UI Add Server Hardware           @{TestData.DLG6ForMonitoring}
    Should be True                      ${rc}       msg=Failed to add Server Hardware for monitoring
    :FOR    ${SERVER}   IN  @{TestData.DLG6ForMonitoring}
    \   ${rc}=                          Fusion UI Refresh Server By Name        ${SERVER.name}
    \   Should be True                  ${rc}       msg=Failed to Refresh Server Hardware
