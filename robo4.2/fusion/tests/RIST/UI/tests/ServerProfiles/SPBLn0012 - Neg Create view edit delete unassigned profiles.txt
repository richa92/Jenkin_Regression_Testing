*** Settings ***
Documentation   SPBLn0012 - Neg Create view edit delete unassigned profiles (Verify can not create server profile with either bad server hardware or bad enclosure group)
Library         robot.api.logger
Resource        ../resource.txt
Test Setup      Load Multi Test Data and Open Browser
Test Teardown	Pause And Close Browser


*** Variables ***
${user}         Administrator


*** Test Cases ***
As an Administrator I want to Verify can not create server profile with either bad server hardware or bad enclosure group
    Fusion UI Login To Appliance        ${user}
    Fusion UI Add Enclosure             @{TestData.enclosure}

    ${data}=                            Get Data By Xpath                       //SPBLn0012/BadSeverHardware/profile
    ${rc}=                              Fusion UI Verify Can Not Create Server Profile With Bad Server Hardware
    ...                                 @{data.profile}
    Should be True                      ${rc}                                   msg=Failed to Verify can not create server profile with bad server hardware

    ${data}=                            Get Data By Xpath                       //SPBLn0012/BadEnclosureGroup/profile
    ${rc}=                              Fusion Ui Verify Can Not Create Server Profile With Bad Enclosure Group
    ...                                 @{data.profile}
    Should be True                      ${rc}                                   msg=Failed to Verify can not create server profile with bad enclosure group