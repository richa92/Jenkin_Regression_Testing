*** Settings ***
Documentation   SPBLn0003 - Edit networkset to create constraint A server profile

Resource    ../resource.txt

Test Setup      Load Multi Test Data and Open Browser
Test Teardown   Pause And Close Browser

*** Variables ***
${user}      Administrator


*** Test Cases ***
Edit networkset to create constraint A server profile
    # Log in using administrator
    Fusion UI Login To Appliance    ${user}

    # Create server profile using duplicate network
    ${data}=                        Get Data By Xpath                       //SPBLn0003/ServerProfile/Create
    ${Status}=                      Fusion UI Create Server Profile    @{data.Create}
    Should Be True                  ${Status}  msg=Failed to create server profile

    # Edit NetworkSet using conflict with existing server profile
    ${data}=                        Get Data By Xpath                       //SPBLn0003/NetworkSets/Edit1
    ${Status}=                      Fusion UI Edit Network Set          @{data.Edit1}
    Should Be True                  ${Status}  msg=Failed to edit networkset

    # Edit server profile using duplicate network
    ${data}=                        Get Data By Xpath                       //SPBLn0003/ServerProfile/Edit
    ${Status}=                      Fusion UI Verify Can Not Edit Server Profile With Error Configuration    @{data.Edit}
    Should Be True                  ${Status}  msg=Failed to create server profile

    # Remove server profile
    ${data}=                        Get Data By Xpath                       //SPBLn0003/ServerProfile/Delete
    ${Status}=                      Fusion UI Delete Server Profile     @{data.Delete}
    Should Be True                  ${Status}  msg=Failed to remove server profile

    # Edit NetworkSet remove unused network
    ${data}=                        Get Data By Xpath                       //SPBLn0003/NetworkSets/Edit2
    ${Status}=                      Fusion UI Edit Network Set          @{data.Edit2}
    Should Be True                  ${Status}  msg=Failed to edit networkset
