*** Settings ***
Documentation   SPBLn0021 - Illegal serial number and UUID

Resource    ../resource.txt

Test Setup      Load Multi Test Data and Open Browser
Test Teardown   Pause And Close Browser

*** Variables ***
${user}      Administrator


*** Test Cases ***
Create server profile with illegal serial number and UUID
    # Log in using administrator
    Fusion UI Login To Appliance    ${user}

    # Create server profile using duplicate connection names
    ${data}=                        Get Data By Xpath                       //SPBLn0021/ServerProfile/Create
    ${Status}=                      Fusion UI Verify Can Not Create Server Profile With Error Configuration     @{data.Create}
    Should Be True                  ${Status}  msg=Failed to create server profile

    # Remove server profile
    ${data}=                        Get Data By Xpath                       //SPBLn0021/ServerProfile/Delete
    ${Status}=                      Fusion UI Delete Server Profile     @{data.Delete}
    Should Not Be True                  ${Status}  msg=Failed to remove server profile
