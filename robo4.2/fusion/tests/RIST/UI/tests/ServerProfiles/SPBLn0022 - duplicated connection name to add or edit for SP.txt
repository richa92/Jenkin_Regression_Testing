*** Settings ***
Documentation   SPBLn0022 -  duplicated connection name to add or edit for SP

Resource    ../resource.txt

Test Setup      Load Multi Test Data and Open Browser
Test Teardown   Pause And Close Browser

*** Variables ***
${user}      Administrator


*** Test Cases ***
duplicated connection name to add or edit for SP
    # Log in using administrator
    Fusion UI Login To Appliance    ${user}

    # Create server profile using duplicate connection names
    ${data}=                        Get Data By Xpath                       //SPBLn0022/ServerProfile/Create1
    ${Status}=                      Fusion UI Verify Can Not Create Server Profile With Error Configuration     @{data.Create1}
    Should Be True                  ${Status}  msg=Failed to create server profile

    # Create server profile
    ${data}=                        Get Data By Xpath                      //SPBLn0022/ServerProfile/Create2
    ${Status}=                      Fusion UI Create Server Profile    @{data.Create2}
    Should Be True                  ${Status}  msg=Failed to create server profile

    # Edit server profile using duplicate connection names
    ${data}=                        Get Data By Xpath                       //SPBLn0022/ServerProfile/Edit
    ${Status}=                      Fusion UI Verify Can Not Edit Server Profile With Error Configuration    @{data.Edit}
    Should Be True                  ${Status}  msg=Failed to create server profile

    # Remove server profile
    ${data}=                        Get Data By Xpath                       //SPBLn0022/ServerProfile/Delete
    ${Status}=                      Fusion UI Delete Server Profile     @{data.Delete}
    Should Be True                  ${Status}  msg=Failed to remove server profile
