*** Settings ***
Documentation   deploy server profile for unassign server from template and override it as SA

Resource        ../resource.txt
Test Setup      Load Multi Test Data and Open Browser
Test Teardown   Pause And Close Browser

*** Variables ***
${user}   Administrator


*** Test Cases ***

 I want deploy server profile for unassign server from template and override it

    Fusion UI Login To Appliance    ${user}
    ${data}=                        get data by xpath                                           //F228p0043/apply
    ${result}=                      Fusion UI Create Server Profile From Template                 @{data.apply}
    Should Be True                  ${result}   Failed to create server profile from template due to error
