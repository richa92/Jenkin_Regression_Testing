*** Settings ***
Documentation   Prepare all necessary resources for running WPST SRM UI test

Resource    ../resource.txt

Test Setup      Load Multi Test Data and Open Browser
Test Teardown   Pause And Close Browser

*** Variables ***
${user}         Administrator

*** Test Cases ***
As an Administrator I want to add license to appliance
    Fusion UI Login To Appliance    ${user}
    ${Status}=          Fusion UI Add License   @{TestData.licenses.license}
    # Should Be True      ${Status}               msg=Failed to add license

As an Administrator I want to add users to my appliance
    Fusion UI Login To Appliance    ${user}
    ${Status}=          Fusion UI Create User   @{TestData.users.user}
    Should Be True      ${Status}               msg=Failed to add users

As a Administrator I want to add a SAN Manager
    Fusion UI Login To Appliance    ${user}
    ${status}=          Fusion UI Add San Manager   @{TestData.sanmanagers.sanmanager}
    Should Be True      ${status}                   msg=Successfully Added A SAN Manager

As an Administrator I want to create all ethernet networks
    Fusion UI Login To Appliance    ${user}
    ${Status}=                      Fusion UI create ethernet network   @{TestData.networks.network}
    Should Be True                  ${Status}                       msg=Failed to add ethernet network

As an Administrator I want to create all FC networks
    Fusion UI Login To Appliance    ${user}
    ${Status}=                      Fusion UI create fc network     @{TestData.fcnetworks.fcnetwork}
    Should Be True                  ${Status}                       msg=Failed to add FC network

As an Administrator I want to add network sets
    Fusion UI Login To Appliance    ${user}
    ${Status}=                      Fusion UI create network set    @{TestData.networksets.networkset}
    Should Be True                  ${Status}                       msg=Failed to add network set

As an Administrator I want to create all of my logical interconnect groups
    Fusion UI Login To Appliance    ${user}
    ${data}=                        Get Data By Xpath   //ligs/lig
    ${Status}=                      Fusion UI Create Logical Interconnect Group      @{data.lig}
    Should Be True                  ${Status}   msg=Failed to add ligs

As an Administrator I want to Create Enclosure Group
    Fusion UI Login To Appliance    ${user}
    ${data}=                        Get Data By Xpath   //encgroups/encgroup
    ${Status}=                      Fusion UI create enclosure group    @{data.encgroup}
    Should Be True                  ${Status}   msg=Failed to add eg
