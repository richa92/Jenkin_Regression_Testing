*** Settings ***
Documentation   Tear down all common resources

Resource    ../resource.txt

Test Setup      Load Multi Test Data and Open Browser
Test Teardown   Pause And Close Browser

*** Variables ***
${user}         Administrator

*** Test Cases ***
As an Administrator I want to remove Enclosure Group
    Fusion UI Login To Appliance    ${user}
    ${data}=                        Get Data By Xpath   //encgroups/encgroup
    ${Status}=                      Fusion UI Delete enclosure group    @{data.encgroup}
    Should Be True                  ${Status}   msg=Failed to remove eg

As an Administrator I want to remove all of my logical interconnect groups
    Fusion UI Login To Appliance    ${user}
    ${data}=                        Get Data By Xpath   //ligs/lig
    ${Status}=                      Fusion UI Delete Logical Interconnect Group      @{data.lig}
    Should Be True                  ${Status}   msg=Failed to remove ligs

As an Administrator I want to remove network sets
    Fusion UI Login To Appliance    ${user}
    ${Status}=                      Fusion UI Delete network set    @{TestData.networksets.networkset}
    Should Be True                  ${Status}                       msg=Failed to remove network set

As an Administrator I want to remove all FC networks
    Fusion UI Login To Appliance    ${user}
    ${Status}=                      Fusion UI Delete fc network     @{TestData.fcnetworks.fcnetwork}
    Should Be True                  ${Status}                       msg=Failed to remove FC network

As an Administrator I want to remove all ethernet networks
    Fusion UI Login To Appliance    ${user}
    ${Status}=                      Fusion UI Delete ethernet network   @{TestData.networks.network}
    Should Be True                  ${Status}                       msg=Failed to remove ethernet network

As a Administrator I want to remove SAN Manager
    Fusion UI Login To Appliance    ${user}
    ${status}=          Fusion UI Remove San Manager   @{TestData.sanmanagers.sanmanager}
    Should Be True      ${status}                   msg=Successfully remove SAN Manager

As an Administrator I want to remove users
    Fusion UI Login To Appliance    ${user}
    ${Status}=          Fusion UI Remove User   @{TestData.users.user}
    Should Be True      ${Status}               msg=Failed to remove users
