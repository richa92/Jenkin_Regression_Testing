*** Settings ***
Documentation		Refresh storage pools from the Fusion Appliance

Resource          	../resource.txt
Test Setup		Load Test Data and Open Browser
Test Teardown		Pause And Close Browser		

*** Variables ***
${StA}      StA

*** Test Cases ***
As an Storage Admin I want refresh storage pools
 	Fusion UI Log into Appliance    ${StA}
 	 	 	 Maximize Browser Window
     :For    ${pool}    in    @{TestData.storagepools}
     \    ${rc}=	Fusion UI refresh storage pool    ${pool.name}
     \ 	Should Be True   ${rc}    msg=Failed to refresh storage pool
