*** Settings ***
Documentation		Edit storage volume templates from the Fusion Appliance

Resource          	../resource.txt
Test Setup		Load Test Data and Open Browser
Test Teardown		Pause And Close Browser

*** Variables ***
${StA}      StA

*** Test Cases ***
As an Storage Admin I want edit storage volume templates
 	Fusion UI Log into Appliance    ${StA}
 	 	 Maximize Browser Window
     :For    ${template}    in    @{TestData.editstoragevts}
     \    ${rc}=	Fusion UI edit storage volume template    ${template.name}
     \ 	Should Be True   ${rc}    msg=Failed to edit storage volume template