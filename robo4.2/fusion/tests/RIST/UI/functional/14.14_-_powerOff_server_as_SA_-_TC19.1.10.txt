*** Settings ***
Documentation		Power off Server Profile Test TC19.1.10

Resource            ../resource.txt
Test Setup			Load Test Data and Open Browser
Test Teardown		Pause And Close Browser
Force Tags	regression   OVA-Qual  DCS  1.05  enclosure-test

***Variables***
${ExpectedStatus}       OK
${ExpectedPowerStatus}  Off
${SA}		SA

*** Test Cases ***

As an SA I want to power off server
    Fusion UI Log Into Appliance   ${SA}
    fusion UI navigate to server profiles page
 	${status}=	fusion UI power off server profile    @{TestData.profiles}
    Should Be True	${status}	Failed power off server profile
 	${status}=	fusion UI verify server profile status	${ExpectedStatus}   @{TestData.profiles}
 	Should Be True	${status}	Server profile status is wrong
    ${status}=	fusion UI verify server profile power status   ${ExpectedPowerStatus}   @{TestData.profiles}
	Should Be True	${status}	Server profile power status is wrong