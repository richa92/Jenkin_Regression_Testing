*** Settings ***
Documentation     Select Mulitple server profiles to PowerOn, PowerOff, and delete
Library           RoboGalaxyLibrary

Resource            ../resource.txt
Suite Setup       Load Test Data and Open Browser
Suite Teardown    Pause And Close Browser For Suite
Force Tags        regression   OVA-Qual  DCS  1.10  enclosure-test

***Variables***
${SA}			SA

*** Test Cases ***
Login as an SA
	Fusion UI Log Into Appliance   ${SA}

As an SA Power On Server Profiles
    ${rc}=      Fusion UI Power On Server Profile      @{TestData.profilegroups}
    Should Be True    ${rc}     msg="Failed to power ON Server Profiles"
    ${rc}=      Fusion UI Verify Server Profile Power Status	On		@{TestData.profilegroups}
    Should Be True    ${rc}     msg="Failed to Verify power ON Status"

As an SA Power Off Server Profiles
    ${rc}=      Fusion UI Power Off Server Profile      @{TestData.profilegroups}
    Should Be True    ${rc}     msg="Failed to power OFF Server Profiles" 
    ${rc}=      Fusion UI Verify Server Profile Power Status	Off		@{TestData.profilegroups}
    Should Be True    ${rc}     msg="Failed to Verify power OFF Status"

As an SA Delete Server Profile
 	${rc}=    Fusion UI Delete Server Profile		@{TestData.profilegroups}
 	Should be Equal		${rc}    ${True}    msg="Failed to delete Server Profiles"

Logout from Fusion
	Fusion UI Logout of Appliance
