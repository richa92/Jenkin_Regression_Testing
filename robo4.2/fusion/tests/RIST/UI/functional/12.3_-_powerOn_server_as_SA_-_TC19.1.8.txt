*** Settings ***
Documentation		Power on Server Profile Test TC19.1.8

Resource            ../resource.txt
Test Setup			Load Test Data and Open Browser
Test Teardown		Logout and close all browsers
Force Tags	regression   OVA-Qual  DCS  1.05  1.10  enclosure-test

***Variables***
${ExpectedStatus}       OK
${ExpectedPowerStatus}  On
${SA}		SA

*** Test Cases ***

As an Administrator I want to power on server profile
    Fusion UI Log Into Appliance   ${SA}
    fusion UI navigate to server profiles page
    fusion UI power on server profile   @{TestData.profiles}
    ${status}=	fusion UI verify server profile status	${ExpectedStatus}   @{TestData.profiles}
    Should Be True	${status}	Failed to power on server profile
    ${status}=	fusion UI verify server profile power status   ${ExpectedPowerStatus}   @{TestData.profiles}
    Should Be True	${status}	Failed to power on server profile
