*** Settings ***
Documentation        Add enclosure TC8.1.0

Resource            ../resource.txt
Test Setup			Load Test Data and Open Browser
Test Teardown		Pause And Close Browser
Force Tags	regression   OVA-Qual  DCS  1.05  setup-enclosure

***Variables***
${SA}		SA

*** Test Cases ***

I want to add enclosure as SA
    Fusion UI Log Into Appliance   ${SA}
    ${status}=    Fusion UI Add Enclosure    @{TestData.enclosures}
    Should Be True    ${status}    msg=Failed to add Enclosures

I want to verify logical interconnect status for newly created enclosure
    Fusion UI Log Into Appliance   ${SA}
	Fusion Ui Verify Logical Interconnects Status	@{TestData.lis}

Rename Server Hardware Types to Expected Values
	Fusion UI Log Into Appliance   ${SA}
	fusion UI navigate to server hardware types page
	${status}=	fusion UI edit server hardware type  @{TestData.hardwareTypes}
	Should Be True    ${status}    msg=Failed to rename server hardware types

