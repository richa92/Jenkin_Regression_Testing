*** Settings ***
Documentation     Add Directory Users and Groups TC3.1.10-14
Resource          ../resource.txt
Suite Setup       Load Test Data and Open Browser
Suite Teardown    Pause And Close Browser For Suite
Force Tags        OVA-Qual    DCS    1.05    user-test

*** Test Cases ***
As Administrator Add LDAP Servers
    [Documentation]    3.1.10 - LDAP - Add LDAP directory
    Log into Fusion appliance as Administrator
    ${Status}=    Fusion UI Add Directory Server    @{TestData.LDAPdomainUsers}[0]
	Should Be True    ${Status}    msg=Failed to add directory server

As Administrator Add AD Servers
    [Documentation]    3.1.12 - AD - Add Active Directory login domain
    ${Status}=    Fusion UI Add Directory Server    @{TestData.ADdomainUsers}[0]
	Should Be True    ${Status}    msg=Failed to add directory server

As Administrator Add AD and LDAP Groups
    [Documentation]    3.1.14 - Add AD or LDAP group
    ${Status}=    Fusion UI Add Directory Users   @{TestData.ADdomainUsers}
	Should Be True    ${Status}    msg=Failed to add directory users
    ${Status}=    Fusion UI Add Directory Users   @{TestData.LDAPdomainUsers}
	Should Be True    ${Status}    msg=Failed to add directory users

#As Administrator Remove AD and LDAP Groups
#    Fusion UI Remove Directory User or Group   @{TestData.LDAPdomainUsers}
#    Fusion UI Remove Directory User or Group   @{TestData.ADdomainUsers}
