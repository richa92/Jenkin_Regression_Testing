*** Settings ***
Documentation		Remove storage pools from Fusion Appliance

Resource          	../resource.txt
Force Tags          user-test
Test Setup			Load Test Data and Open Browser
Test Teardown		Pause And Close Browser

***Variables***
${user}           StA

*** Test Cases ***
As an SA I want add storage pool to Fusion
 	Fusion UI Log Into Appliance    ${user}
 	:FOR    ${storagePool}    IN    @{TestData.storagepools}
 	\    ${rc}=    Fusion UI remove storage pool	${storagePool.name}
 	\    Should be True    ${rc}    msg=Failed to remove Storage Pool
