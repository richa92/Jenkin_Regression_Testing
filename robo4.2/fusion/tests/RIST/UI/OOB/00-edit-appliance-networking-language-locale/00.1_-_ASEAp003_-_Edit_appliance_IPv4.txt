*** Settings ***
Documentation   Add license OneView
Resource  ../resource.txt
Test Teardown   Pause Test Execution and Relogin Appliance On Failure

*** Test Cases ***
ASEAp003 - Edit appliance IPv4
    ${data}=            Get Data By Xpath   //editnetworking/networking
    :FOR    ${networking}    IN    @{data.networking}
    \     ${host}=            fusion_ui_get_appliance_networking
    \     set_value_for_property       ${networking}     hostname    ${host[0]}
    \     Run Keyword If     '${networking.ipv4assignment}' == 'manual'        set_value_for_property       ${networking}     ipv4address    ${host[1]}
    \     ${Status}=                      Fusion UI Edit C7000 Networking   ${networking}
    \     Should Be True                  ${Status}   msg=Failed to edit networking
    \     ${Status}=                      Fusion UI Verify C7000 Networking   ${networking}
    \     Should Be True                  ${Status}   msg=Failed to verify networking
