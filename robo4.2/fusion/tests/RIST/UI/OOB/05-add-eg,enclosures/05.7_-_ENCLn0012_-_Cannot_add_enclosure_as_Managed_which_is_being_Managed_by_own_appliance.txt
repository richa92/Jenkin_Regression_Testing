*** Settings ***
Documentation   Cannot add enclosure as Managed which is being Managed by itself
Resource        ../resource.txt
Test Teardown   Pause Test Execution and Relogin Appliance On Failure
# Suite Teardown  Remove Enclosure

*** Test Cases ***
ENCLn0012 - Cannot add enclosure as Managed which is being Managed by itself
    Reload Page
    ${data}=                        Get Data By Xpath   //re-addenclosure/enclosure
    Set Global Variable             ${custom_enclosure_obj}    ${data}
    ${rc}=                          Fusion UI Validate Should Not Readd Enclosure           @{data.enclosure}
    Should Be True                  ${rc}               Failed to validate appliance should not re-add managed enclosure which already managed by itself
    Reload Page

# *** Keywords ***
# Remove Enclosure
#     ${status}=                      Fusion UI Remove Enclosure      @{custom_enclosure_obj.enclosure}
#     Should Be True                  ${status}                       Failed to remove enclosures