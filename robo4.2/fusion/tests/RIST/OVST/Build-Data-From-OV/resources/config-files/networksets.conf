# variable-keys mapping
# top level or members-level keys
membersKeys = ['type', 'name', 'nativeNetworkUri', 'networkUris']
expectedKeys = ['category', 'state', 'description', 'uri', 'status']

# no subkeys needed at this point...