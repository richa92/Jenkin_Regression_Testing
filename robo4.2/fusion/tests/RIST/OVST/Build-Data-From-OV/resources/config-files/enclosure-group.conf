# variable-keys mapping
# top level (or members-level)
membersKeys = ['name', 'type', 'enclosureTypeUri', 'stackingMode', 'interconnectBayMappingCount', 'interconnectBayMappings', 'ipAddressingMode', 'enclosureCount']

expectedKeys = ['uri', 'description', 'category', 'state', 'status', 'powerMode', 'portMappingCount', 'portMappings', 'associatedLogicalInterconnectGroups']
# membersKey that has sub-keys (format is <key>Keys)
interconnectBayMappingsKeys = ['interconnectBay', 'logicalInterconnectGroupUri', 'enclosureIndex']

# members dictionary defining the subkeys
membersDict = {
    'interconnectBayMappings': interconnectBayMappingsKeys,
    'interconnectBayMappings_type': 'listOfDict'
}

# preserve uri value(s) list
keepUris = []

# for negative conditional keys. See resources/profiles_variable_keys_map.txt, for example.
globalNegativeCondDict = {}
nonglobalNegativeCondDict = {}
