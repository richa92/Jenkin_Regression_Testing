# variable-keys mapping
# top level (or members-level)
membersKeys = ['description', 'isShareable', 'name', 'provisioningType', 'size', 'storagePool']
expectedKeys = ['uri', 'state', 'status', 'type']
