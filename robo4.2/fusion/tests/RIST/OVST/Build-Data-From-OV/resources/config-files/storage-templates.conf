# variable-keys mapping
# toplevel (or members-level)
membersKeys = ['name', 'description', 'properties']
expectedKeys = ['category', 'state', 'type', 'uri', 'status']

# membersKey that has sub-keys (format is <key>Keys)
propertiesKeys = ['description', 'name', 'snapshotPool', 'storagePool', 'provisioningType', 'isShareable', 'size']

# members dictionary defining the subkeys
membersDict = {
    'properties': propertiesKeys,
    'properties_type': 'dictOfDict'
}
