*** Settings ***
Documentation   Remove storage volumes to Fusion Appliance
...             (also covers a portion of TC21.1.16: Delete SAN Server Profiles as SA and delete Volumes)

Resource        ../resource.txt
Test Setup      Load Test Data and Open Browser
Test Teardown   Pause And Close Browser


*** Variables ***
${SA}   SA


*** Test Cases ***
#As an Administrator I want remove storage volumes to Fusion
#   Fusion UI Login To Appliance    ${SA}
#   :For    ${volume}               In          @{TestData.storagevolumes}
#   \       Fusion UI Delete storage volumes    ${volume.name}      ApplianceAndStorage

As an Administrator I want remove storage volumes to Fusion
    Fusion UI Login To Appliance    ${SA}
    ${status_new_volumes}=          Fusion UI Delete storage volumes                    @{TestData.storagevolumes}
    ${status_existing_volume}=      Fusion UI Delete storage volumes                    @{TestData.storagevolumesAdd}
    Should Be True                  ${status_new_volumes}&${status_existing_volume}     msg=Failed to remove storage volumes

