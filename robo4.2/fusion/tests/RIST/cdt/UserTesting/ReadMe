UserTesting directory is a proof of concept for executing the same test case example (Power On Server) with multiple
user accounts in parallel.

Execution example:
$ cd UserTesting/
$ pabot --processes 5 --pabotlib --variablefile ../data/dcs_data.py -L TRACE .

NOTE: Any Robot Framework parameters should be grouped together after any pybot specific parameters.