*** Settings ***
Documentation        Partial Factory reset
Resource             ../Resource/CIM_CommonResource.txt
Resource             ../Resource/CIM_Activity.txt
Test Setup           Load Test Data and Open Browser
Test Teardown        Logout and close all browsers
Variables            ../../../../FusionLibrary/ui/business_logic/general/activity_elements.py

*** Variables ***
${user_name}        Administrator
${password}         hpvse123
${searchtext}       Appliance reset to factory defaults

*** Test Cases ***
As an Administrator I want to perform Partial Factory Reset
    [Documentation]    Perform Appliance Partial Factory Reset
    Log into Fusion appliance as Administrator
    ${blnAppReset} =    Fusion UI Appliance Factory Reset    ${user_name}    ${password}
    Should Be Equal As Strings    ${blnAppReset}    ${blnExpected}    msg=Fail to perform Appliance factory Reset

    #Verify entry in auditlog file
    ${filepath}=    Download and unzip Audit log file    ${DownloadPath}
    ${bln_logentry_exists} =    file_contains    ${filepath}    ${searchtext}
    Should Be Equal As Strings    ${bln_logentry_exists}    ${blnExpected}    msg=Entry ${searchtext} not available in log file.
