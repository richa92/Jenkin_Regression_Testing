*** Settings ***
Library              Collections
Documentation        TBird PM SAN Tests

Resource             ../resources/resource.txt
Suite Setup          Run Keywords
...                     Run Suite Setup
...                     Delete All Storage Volumes
...                     PM SAN Pre Test
Suite Teardown       Run Keywords
...                     Logout and close all browsers
...                     Delete All Storage Volumes
Test Setup           Validate Skip Conditions

*** Variables ***
${DataFile}         ../data/test_data.xml

*** Test Cases ***
TEST 1: Add a TBird Unassigned Profile With SAN Volumes
    ${status}=           Fusion UI Only Profile Create Server Profile    @{TestData.unassignedprofilewithsanvolume}
    Run Keyword If      '${status}' == 'False'       Fail                FAIL: Error creating server profile.
    [Teardown]           Skip Suite If Failure          ${TEST NAME}

TEST 2: Edit a TBird Unassigned Profile With SAN Volumes
    ${status}=           Fusion UI Edit Server Profile    @{TestData.editprofilewithsanvolume}
    Run Keyword If      '${status}' == 'False'       Fail                FAIL: Error editing server profile.

TEST 3: Copy a TBird Unassigned Profile With SAN Volumes
    ${status}=           Fusion UI Only Profile Copy Server Profile    @{TestData.copytbirdprofilesunassignedwithsan}
    Run Keyword If      '${status}' == 'False'       Fail                FAIL: Error copying server profile.

TEST 4: Delete a TBird Unassigned Profile With SAN Volumes
    ${status}=           Fusion UI Delete Server Profile    @{TestData.deletetunassignedwithsan}
    Run Keyword If      '${status}' == 'False'       Fail                FAIL: Error deleting server profile.