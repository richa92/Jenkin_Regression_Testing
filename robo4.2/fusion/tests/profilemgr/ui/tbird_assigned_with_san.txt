*** Settings ***
Library              Collections
Documentation        TBird PM SAN Tests

Resource             ../resources/resource.txt
Suite Setup          Run Keywords
...                     Run Suite Setup
...                     Delete All Storage Volumes
...                     PM SAN Pre Test
Suite Teardown       Run Keywords
...                     Logout and close all browsers
...                     Delete All Storage Volumes
Test Setup           Validate Skip Conditions

*** Variables ***
${DataFile}         ../data/test_data.xml

*** Test Cases ***
TEST 1: Add a TBird Assigned Profile With SAN Volumes
    ${status}=           Fusion UI Only Profile Create Server Profile    @{TestData.profilewithsanvolume}
    Run Keyword If      '${status}' == 'False'       Fail                FAIL: Error creating server profile.
    [Teardown]           Skip Suite If Failure          ${TEST NAME}

TEST 2: Edit a TBird Assigned Profile With SAN Volumes
    ${status}=           Fusion UI Edit Server Profile    @{TestData.editassignedprofilewithsanvolume}
    Run Keyword If      '${status}' == 'False'       Fail                FAIL: Error copying server profile.

TEST 3: Copy a TBird Assigned Profile With SAN Volumes
    ${status}=           Fusion UI Only Profile Copy Server Profile    @{TestData.copytbirdprofilesassignedwithsan}
    Run Keyword If      '${status}' == 'False'       Fail                FAIL: Error updating server profile.

TEST 4: Delete a TBird Assigned Profile With SAN Volumes
    ${status}=           Fusion UI Delete Server Profile    @{TestData.deletetassignedwithsan}
    Run Keyword If      '${status}' == 'False'       Fail                FAIL: Error deleting server profile.