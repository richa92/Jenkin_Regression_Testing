*** Settings ***
Documentation     Delete Ethernet Network
...    = Usage =
...    | pybot | -v FUSION_IP:<Your-IP> | -v NET_NAME:<name> | del_net.txt |
...    = Variables =
...    | FUSION IP | Required; IP address of the FusionVM to use | 
...    | NET_NAME | Optional; default *NW1* |
Library           RoboGalaxyLibrary         # DVTs Robot Framework extensions
Library           FusionLibrary                  # DVTs Robot Framework extensions
Variables         resources/variables.py    ${FUSION_IP}    # Comet-DVT default variables 
Resource          resources/fusion.txt      # Comet-DVT keyword extensions
Force Tags        CometUtil                 # All test cases in sub test suites get these tags
Suite Setup       Login to Fusion Via REST    
Suite Teardown    Logout of Fusion Via REST

*** Variables ***
${NET_NAME}       NW1

*** Test Cases ***
Delete Ethernet Network
    ${Network}     Get Ethernet Network By Name    ${NET_NAME}
    ${VlanId}      Get From Dictionary    ${Network}    vlanId
    ${Response}    Delete Ethernet Network By Id   ${VlanId}
    Check Response    ${Response}

Verify Deleted Ethernet Network
    Run Keyword and Expect Error    Cant find ethernet-network named ${NET_NAME}: 0.0 != 1.0
    ...    Get Ethernet Network By Name    ${NET_NAME}
