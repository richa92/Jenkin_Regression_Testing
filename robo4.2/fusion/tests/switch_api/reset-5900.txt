*** Settings ***
Documentation    Reset HP 5900 AF switch to factory settings.
...    = USAGE =
...    | pybot | -L debug | -v SWITCH_IP:xx.xx.xx.xx | reset_5900.txt | 
...    = Variables =
...    | SWITCH IP | Required; IP address or name of the switch to use | 
...    | SERIAL_IP | Optional; Ip address of server for serial connection. |
...    | SERIAL_DL_USERNAME | Optional; Server for serial connection. Defaults to root |
...    | SERIAL_DL_PASSWORD | Optional: Password to serial connection server. Defaults to Hpinvent1 |

Library       RoboGalaxyLibrary         # DVTs Robot Framework extensions
Variables     resources/variables.py    unused    ${SWITCH_IP}    # Comet-DVT default variables
Resource      resources/switch.txt      # Comet-DVT keyword extensions
Force Tags    CometUtil                 # All test cases in sub test suites get these tags

*** Variables ***

*** Test Cases ***


Reset a 5900 switch
    Log Many    ${SWITCH_IP}    ${SERIAL_DL_IP}    ${SERIAL_DL_USERNAME}    ${SERIAL_DL_PASSWORD}
    Reset 5900 DL    ${SERIAL_DL_IP}    ${SERIAL_DL_USERNAME}    ${SERIAL_DL_PASSWORD}



Collect 5900 Switch Version Metadata
    Set Switch Version Metadata
