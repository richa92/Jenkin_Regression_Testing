*** Settings ***
Resource          resources/fusion_setup_resource.txt
Resource          resources/switch_setup_resource.txt


*** Variables ***
${FUSION VM}      10.0.12.63
${USERNAME}       Administrator
${PASSWORD}       hpvse123
${MODULE TYPE}    HP Virtual Connect FlexFabric 10Gb/24-Port Module
${SWITCH IP}      16.119.102.221
${DL360}          16.119.102.227
${ROOT}           root
${DL ROOT PASS}  hpvse123
${FVM ROOT PASS}  hpvse123
${ROOT PASS}   hpvse1

*** Test Cases ***
#STEP 1 Return the Enclosure to Default state
#    Login to Fusion VM SSH    ${FUSION VM}    ${ROOT}    ${ROOT PASS}
#    Reset Hydrogen Enclosure to Factory Default
#    Restablish The CIC After Factory Reset
#    Logout

#STEP 2 Assure properties file is in place
#    Login to Fusion VM SSH    ${FUSION VM}    ${ROOT}    ${ROOT PASS}
#    Verify Properties file is in place
#    Logout

#STEP 3 Return the switch to FACTORY Default
#    Login to DL360    ${DL360}    ${ROOT}    ${DL ROOT PASS}
#    Reset the 5900-Switch to Factory Default and install configuration
#    Logout

#STEP 4 Assure the Staging directory is clean
#    Login to Fusion VM SSH    ${FUSION VM}    ${ROOT}    ${ROOT PASS}
#    Verify the staging directory is clean
#    Logout

#STEP 5 Check Interconnect Config
#    #Login to Fusion VM SSH    ${FUSION VM}    ${ROOT}    ${ROOT PASS}
#    #Login to 5900 Switch SSH    ${SWITCH IP}    ${USERNAME}    ${PASSWORD}
#    #Display switch current configuration
#    #Logout of 5900 Switch
#    Logout    #logout of 5900 Switch
#    Logout    #logout of Fusion VM

Fusion GUI Login
    Login to Fusion VM GUI    ${FUSION VM}    ${USERNAME}    ${PASSWORD}

Create 6 Ethernet Networks
    Add Network    NW1    101
    Add Network    NW2    102
    Add Network    NW3    103
    Add Network    NW4    104
    Add Network    NW5    105
    Add Network    NW6    106

Create Logical Uplink Group
    Create Logical Uplink Group    helium lig
    Add Interconnect    1    ${MODULE TYPE}
    Add Interconnect    3    ${MODULE TYPE}
    Add Uplink Set    UL1    ethernet
