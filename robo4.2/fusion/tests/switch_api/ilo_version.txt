*** Settings ***
Documentation     Fetch ILO version string
...    = Usage =
...    | pybot | -v SWITCH_IP:<Your-IP> | ilo_version.txt |
...    = Variables =
...    | SWITCH IP | Required; IP address of the FusionVM to use |
Library           RoboGalaxyLibrary           # DVTs Robot Framework extensions
Variables         resources/variables.py      unknown    ${SWITCH_IP}    # Comet-DVT default variables 
Resource          resources/server.txt        # Comet-DVT keyword extensions
Force Tags        CometUtil                   # All test cases in sub test suites get these tags
#Suite Setup       Login to Server ILO Via SSH
#Suite Teardown    Logout of Server Via SSH

*** Variables ***

*** Test Cases ***
Set iLO Version Metadata
    [Documentation]    Get the version ident string from Fusion
    Login to Server iLO Via SSH
    ${Version}    ${Date}    Get ILO Version Information
    Logout of Server iLO Via SSH
    Set Suite Metadata     Server iLO Version    ${Version} : ${Date}    top=True
    