*** Settings ***
Documentation			Create a profile with 3 connections. Verify correct auto-add and button states in san storage section
Resource				../resource.txt
Suite Setup				Run Suite Setup
Suite Teardown			Logout and Close Window

*** Variables ***
${DataFile}			./test_data.xml

*** Test Cases ***
Test 1: Create a profile with 3 connections, and add a new volume
	${profile_create_msgs}=		Fusion UI Create San Storage Profile		@{TestData.profiles}
	Log		${profile_create_msgs}
	Should Be Empty		${profile_create_msgs}
	Fusion UI Verify Volume Configuration For Profile		profile_with_3_connections  @{TestData.volumes_1}
	Get Clean Slate

Test 2: Edit the previous profile, and add the third connection to the volume
	${profile_edit_msgs}=		Fusion UI Edit San Storage Profile			@{TestData.editprofiles}
	Log		${profile_edit_msgs}
	Should Be Empty		${profile_edit_msgs}
	Fusion UI Verify Volume Configuration For Profile		profile_with_3_connections  @{TestData.volumes_2}
	Get Clean Slate

Test 3: Delete the profile
	Fusion UI Delete Pmsan Server Profile			@{TestData.deleteprofiles}