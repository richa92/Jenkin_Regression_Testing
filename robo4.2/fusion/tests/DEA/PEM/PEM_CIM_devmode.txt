*** Settings ***
Documentation     Pre requisite script to convert CIM into Devmode to exeucte the PEM commands on OV 3.10
...    = Usage =
...    | pybot | -L DEBUG | APPLIANCE_IP:<fusion_ipv4> | Stand_By_Cim_IP:<Stand_by_CIM_ipv4> | PEM_CIM_Devmode.txt | 
...    = Variables =
...    | GIT_REPO_ROOT | Required; Repo root path if NOT defined in environment variable|
...    | RESOURCE_ROOT | Optional: Resource files path if NOT defined in environment variable|
...    | VARIABLE_ROOT | Optional: Variable files path if NOT defined in environment variable|
...    | APPLIANCE IP     | Required; OneView IP address under test | 
...    | Stand_By_Cim_IP  | Required; If no standyby CIM then keep blank i.e Stand_By_Cim_IP: 

Library           RoboGalaxyLibrary            # DVTs Robot Framework extensions
Library           FusionLibrary                # DVTs Robot Framework extensions
Library           OperatingSystem
Library           MgmtFWLibrary
Library           loginPEMClass 
Library           json
Library           String
Library           Collections

Variables         ${GIT_REPO_ROOT}${VARIABLE_ROOT}/dea_variables.py
Variables         ${GIT_REPO_ROOT}${VARIABLE_ROOT}/data_variables.py
Variables         ${GIT_REPO_ROOT}/mgmtfw/tests/thunderbird/variables/tBird_variables.py
Resource          ${GIT_REPO_ROOT}${RESOURCE_ROOT}/resource_all.txt

Suite Setup       Run Keywords    Login to Fusion via SSH
Suite Teardown    Run Keywords    Logout of Fusion Via SSH

*** Test Cases ***
TC 01: PEM Convert Active CIM into devmode: To convert the Active CIM into Devmode
    [Documentation]    Test Case to convert the Active CIM into Devmode
    [Tags]    Automated   DEA    API    PEM    OV3.10
    Convert CIM into DevMode  ${APPLIANCE_IP}  Active     # Convert the active CIM into devmode
 
TC 02: PEM Convert StandBy CIM into devmode: To convert the StandBy CIM into Devmode
    [Documentation]    Test Case to convert the StandBy CIM into Devmode
    [Tags]    Automated   DEA    API    PEM    OV3.10
    ${standy_by_cim_status}=  Run Keyword And Return Status  Should Not Be Empty  ${Stand_By_Cim_IP}
    Run Keyword If    '${standy_by_cim_status}' == 'True' 
    ...  Convert CIM into DevMode    ${Stand_By_Cim_IP}  Standby     # Convert the StandBy CIM into devmode