*** Settings ***

Documentation     pybot -L Trace -v APPLIANCE_IP:<APP_IPv4-IPv6> 
...               -v Enclosures:<Encl_Name> -v GIT_REPO_ROOT:<ROOT_DIR> -v SupportDump_Path:local_path Tbird_Adapters_SupportDump.txt
...               Fill in command required field for APPLIANCE_IP, Enclosures and optional supportdump path.
Variables         ${GIT_REPO_ROOT}/fusion/tests/DEA/variables/dea_variables.py
Variables         ${GIT_REPO_ROOT}${VARIABLE_ROOT}/data_variables.py
Resource          ${GIT_REPO_ROOT}${RESOURCE_ROOT}/resource_all.txt

Library           RoboGalaxyLibrary
Library           FusionLibrary
Library           Collections
Library           OperatingSystem
Library           DateTime

Suite Setup       Run Keywords    Login to Fusion Via REST
Suite Teardown    Run Keyword    Logout of Fusion Via REST

*** Test Cases ***

Archive Support Dump After Regression Test Run
    [Documentation]    Create support dump after regression test run
    [Tags]    support-dump
    ${Curr_Date}=    Get Current Date    result_format=%Y_%m_%d_%H_%M
    ${Support_File_name}=   Set Variable   ${SupportDump_Path}\\${Enclosures}_Support_Dump_${Curr_Date}.sdmp
    Run Keyword    Create Directory    ${SupportDump_Path}
    Create and download support dump    ${Support_File_name}