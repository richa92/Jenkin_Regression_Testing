*** Settings ***
Documentation	i3s Login for deleting the GI
Resource		i3sUIConfig.txt
Force Tags		Buildup
Test Setup		Load Test Data and Open Browser
#Test Teardown	Logout And Close All Browsers


*** Variables ***
${DataFile}	./i3sUIData.xml

*** Test Cases ***
Test 1 : Deleting GI from UI
	${user}=	Get Data By Property	${TestData.users}	name	Administrator
	log to console	${user}
	i3S UI Login to Appliance	${user[0].name}
	Log to Console	Test Step - 1 "Login Completed"
	#Different tests to Delete OEDP through UI	
	${Response}=	I3S UI DELETE GOLDENIMAGE  @{TestData.removegoldenimageuis}
	Log	${Response}
	Log to Console  Test Step - 2 "Delete Completed"
	i3S UI Logout of Appliance
	Log to Console  Test Step - 3 "Logout Completed"
	